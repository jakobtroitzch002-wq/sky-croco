BROWSER-ONLY BUILD
Upload these files to a new GitHub repository. Open Actions and run "Build Crocodilandy Sky".
Download artifact "Crocodilandy-Sky-Mod" when the run is green.

This project is a compilable Fabric 26.2 starter. The full requested custom ChunkGenerator still requires
version-specific implementation and testing; a successful build does not by itself mean the island world is implemented.
